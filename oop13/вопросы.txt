1. Почему используется тип данных tuple для хэширование, а не list
потому что tuple неизменяем, а list изменяем
2. Почему важно переопределять метод __hash__, если переопределён __eq__?
это позволяет корректно сравнивать объекты

